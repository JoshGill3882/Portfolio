# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# File altered to fit needs more precisely

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - prod

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup node
        uses: actions/setup-node@v3

      - name: Clean Install
        run: npm ci

      - name: Build Repo
        run: npm run build

  push-backup:
    needs: build
    name: Push Image to Docker Hub for Backup
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Build and Push to Docker Hub
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/portfolio:v0

  deploy-to-firebase:
    needs: push-backup
    name: Deploy To Firebase Hosting
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_9D3B6 }}'
          channelId: live
          projectId: portfolio-9d3b6